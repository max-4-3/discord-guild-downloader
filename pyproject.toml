[project]
name = "discord-guild-downloader"
version = "1.0.0"
description = "Python script designed to download various information from a discord guild (server)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.9.3",
    "alive-progress>=3.1.5",
    "colorama>=0.4.6",
    "fake-useragent>=2.2.0",
]
